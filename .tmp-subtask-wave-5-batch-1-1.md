You are a focused implementation agent. Implement exactly what this prompt describes. Nothing more, nothing less. Do NOT read or reference any other plan document, roadmap, or task files.

Load and apply these skills using the Skill tool before writing any code: `rust-services:production-code-recipe`

Working directory: `/Users/andreas.pohl/workspace/code/plan-executor/.my/worktrees/remote-execution`

---

## Task: Final integration test, clippy cleanup, and binary verification

### What to do

#### Step 1: Run the full test suite

Run: `cargo test`

Expected: All tests pass, including:
- `plan_execution_mode_test` (4 tests)
- `config_remote_repo_test` (3 tests)
- `remote_test` (3 tests)
- All pre-existing tests

If any NEW test failures occur (not pre-existing ones like `handoff_test` or `pricing_test`), fix them.

#### Step 2: Run clippy

Run: `cargo clippy -- -D warnings`

Expected: No warnings. If there are warnings, fix them. Common issues to watch for:
- Unused imports in `cli.rs`
- Unused variables (e.g., `child` variable in foreground mode — suppress with `let _ = child;`)
- Missing `use` statements for `anyhow::Context` in `cli.rs`
- Clippy suggestions for `remote.rs` (e.g., `map` vs `and_then`, redundant closures)

Fix ALL clippy warnings, even in files you didn't create — the goal is zero warnings from `cargo clippy -- -D warnings`.

#### Step 3: Verify the binary runs

Run: `cargo run -- --help`
Expected: Shows help with `execute` (including `-f` flag description), `remote-setup`, and all existing commands.

#### Step 4: Commit any fixes

If you made any changes in steps 1-3:
```bash
export GPG_TTY=$(tty)
gpg-agent --daemon 2>/dev/null || true
git add -A
git commit -m "chore: fix clippy warnings and finalize remote execution integration"
```

If no changes were needed, report that everything is clean.

### Out of scope

Do NOT add new features. Do NOT refactor existing code beyond what is needed to fix test failures or clippy warnings. Do NOT add new tests. Only fix issues found by the test suite and clippy.

---

After completing the task, report: all files you modified (if any), the result of `cargo test`, the result of `cargo clippy -- -D warnings`, and the result of `cargo run -- --help`.
