//! Concrete `Step` implementations grouped by `JobKind`.
//!
//! Phase A only contains stub shells under `plan`. Real bodies arrive in
//! Phase A2.2 (delegation) and Phase D (preflight).

pub mod plan;
